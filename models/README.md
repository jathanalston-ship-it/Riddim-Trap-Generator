# models — Local AI Models
ONNX Runtime + DirectML wrapper (`runtime/`), shipped models (`packaged/`: sound-quality rater, audio embedder, genre-fit classifier — small discriminative models only), and offline training scripts (`training/`, not shipped). CPU fallback always available; fully offline.
