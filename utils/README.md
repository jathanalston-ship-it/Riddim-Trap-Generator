# utils — Utilities
Foundation layer (no internal dependencies): seeded PRNG tree (PCG64 named streams), job system, SIMD helpers, logging, profiling, file I/O, audio units (dB/LUFS/semitones), hashing/ids.
