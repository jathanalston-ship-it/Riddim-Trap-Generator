# database — Sample Database (Storage)
SQLite access layer + versioned migrations, HNSW vector index over audio embeddings, and the content-addressed FLAC blob store. Zero-admin, fully local. See docs/architecture/03-technology-stack.md.
