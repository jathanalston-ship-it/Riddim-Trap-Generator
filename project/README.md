# project — Project Files
Versioned `.rtgproj` schema (JSON), save/load, asset pinning by content hash (projects reopen bit-identically), export bundles. A v1 project must open in every future version.
