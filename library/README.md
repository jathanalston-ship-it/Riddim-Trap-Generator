# library — Sound Library
Asset service: ingestion (recipe + audio + metadata + embedding + lineage), hybrid query API (metadata filters + vector similarity + fitness ranking), favorites, and the background evolution scheduler (mutate / breed / combine / prune). See docs/architecture/06-library-evolution.md.
