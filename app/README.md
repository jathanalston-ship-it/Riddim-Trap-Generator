# app — Application Shell
Entry point, main window, application lifecycle, module registry, and job-system bootstrap. Packaging/installer scripts live in `installer/`. Depends on every engine module; nothing depends on it. See docs/architecture/01-master-architecture.md.
