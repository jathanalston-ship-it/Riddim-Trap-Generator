# tests — Tests
`unit/` mirrors engine modules; `integration/` runs params+seed → audio; `golden/` pins seeds to render checksums (the determinism contract); `audio_quality/` checks LUFS/true-peak/spectral conformance; `benchmarks/` track render speed and query latency.
