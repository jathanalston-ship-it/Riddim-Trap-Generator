# engine/render — Audio Renderer
Offline faster-than-realtime block renderer (symbolic score + resolved sounds + automation → per-track float audio), realtime preview graph, resampling, dithered WAV/FLAC/MP3 export, stem export.
