# engine/sound_design — Sound Design Core
Shared synthesis engine: recipe node-graph runtime (`graph/`), DSP nodes (`nodes/`: FM, wavetable, granular, noise, comb, distortion, multiband, resampler…), feature extraction (`analysis/`), and the candidate rating pipeline (`rating/`). See docs/architecture/05-sound-design-engine.md.
