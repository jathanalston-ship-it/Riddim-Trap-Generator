# engine/generation — Generation Engine (Orchestrator)
Staged, cancellable pipeline controller: Plan → Compose → Sound Selection/Design → Render → Mix → Master → Library update. Persists per-stage artifacts to support resume, partial re-roll, and History. See docs/architecture/01-master-architecture.md §4.2.
